<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= category %> Menu</title>
  </head>
  <body>
    <%- include('./partials/nav') %>

    <h1><%= category %></h1>
    <ul>
      <% menuItems.forEach(element => { %>
      <li>
        <%= element.name %> $<%= element.price %> - <%= element.details %>
        (Rating: <%= element.rating %>)
      </li>
      <% }) %>
    </ul>
  </body>
</html>
